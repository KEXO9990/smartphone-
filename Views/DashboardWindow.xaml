<Window x:Class="MobileShopApp.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dashboard" Height="600" Width="800" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="📊 Dashboard" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="#2c3e50"/>

        <!-- Stats Cards -->
        <UniformGrid Grid.Row="1" Rows="1" Columns="3" Margin="0,0,0,20">
            <Border Background="#3498db" CornerRadius="5" Margin="5" Padding="15">
                <StackPanel>
                    <TextBlock Text="📦" FontSize="24" HorizontalAlignment="Center" Foreground="White"/>
                    <TextBlock x:Name="TotalProductsText" Text="0" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                    <TextBlock Text="Total Products" FontSize="12" HorizontalAlignment="Center" Foreground="White"/>
                </StackPanel>
            </Border>
            
            <Border Background="#e74c3c" CornerRadius="5" Margin="5" Padding="15">
                <StackPanel>
                    <TextBlock Text="⚠️" FontSize="24" HorizontalAlignment="Center" Foreground="White"/>
                    <TextBlock x:Name="LowStockCountText" Text="0" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                    <TextBlock Text="Low Stock Items" FontSize="12" HorizontalAlignment="Center" Foreground="White"/>
                </StackPanel>
            </Border>
            
            <Border Background="#27ae60" CornerRadius="5" Margin="5" Padding="15">
                <StackPanel>
                    <TextBlock Text="💰" FontSize="24" HorizontalAlignment="Center" Foreground="White"/>
                    <TextBlock x:Name="TodaySalesText" Text="$0.00" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
                    <TextBlock Text="Today's Sales" FontSize="12" HorizontalAlignment="Center" Foreground="White"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Low Stock Alert -->
        <Border Grid.Row="2" BorderBrush="#e74c3c" BorderThickness="2" CornerRadius="5" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="⚠️ Low Stock Alerts" FontSize="16" FontWeight="Bold" Foreground="#e74c3c" Margin="0,0,0,10"/>
                
                <DataGrid Grid.Row="1" x:Name="LowStockDataGrid" AutoGenerateColumns="False" 
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product Name" Binding="{Binding Name}" Width="200"/>
                        <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="100"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="100"/>
                        <DataGridTextColumn Header="Current Stock" Binding="{Binding Quantity}" Width="100"/>
                        <DataGridTextColumn Header="Alert Threshold" Binding="{Binding LowStockThreshold}" Width="120"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
